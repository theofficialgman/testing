name: Test CI
# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          sudo apt update
          sudo apt install tree -y
          tree /
          ls -l /home/actions/temp/scripts
